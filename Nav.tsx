'use client';
import {useState} from 'react'; import {Menu,X,ArrowUpRight} from 'lucide-react';
const links=[['Inventory','#inventory'],['Sell','#sell'],['Services','#services'],['Partners','#partners'],['Investors','#investors']];
export default function Nav(){const [open,setOpen]=useState(false);return <header className="fixed top-0 z-50 w-full border-b border-white/10 bg-black/45 backdrop-blur-xl"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5 lg:px-8"><a href="#top" className="font-display text-xl font-bold tracking-tight">VELORA<span className="text-gold">.</span></a><nav className="hidden gap-7 text-sm text-white/65 md:flex">{links.map(([x,y])=><a key={x} className="transition hover:text-white" href={y}>{x}</a>)}</nav><a href="#contact" className="hidden items-center gap-2 rounded-full bg-gold px-5 py-2.5 text-sm font-bold text-black md:flex">Get started <ArrowUpRight size={16}/></a><button className="md:hidden" onClick={()=>setOpen(!open)} aria-label="Toggle menu">{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t border-white/10 bg-black px-5 py-5 md:hidden">{links.map(([x,y])=><a onClick={()=>setOpen(false)} className="block border-b border-white/10 py-4 text-white/80" key={x} href={y}>{x}</a>)}<a onClick={()=>setOpen(false)} className="mt-4 block rounded-full bg-gold px-5 py-3 text-center font-bold text-black" href="#contact">Get started</a></div>}</header>}
