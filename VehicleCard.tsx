'use client';
import Image from 'next/image'; import {ArrowUpRight,Gauge} from 'lucide-react'; import {Vehicle} from '@/data/inventory';
export default function VehicleCard({v}:{v:Vehicle}){return <article className="group min-w-[310px] overflow-hidden rounded-3xl border border-white/10 bg-[#111] sm:min-w-[360px]"><div className="relative h-56 overflow-hidden"><Image src={v.image} alt={`${v.year} ${v.name}`} fill className="object-cover transition duration-700 group-hover:scale-105" sizes="400px"/><div className="absolute left-4 top-4 rounded-full bg-black/60 px-3 py-1 text-[10px] font-bold tracking-[.18em] backdrop-blur">{v.tag}</div></div><div className="p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs text-white/40">{v.year} · {v.mileage}</p><h3 className="mt-1 font-display text-lg font-bold">{v.name}</h3></div><p className="font-display font-bold">${v.price.toLocaleString()}</p></div><div className="mt-5 flex items-center gap-4 text-xs text-white/45"><span className="flex items-center gap-1"><Gauge size={14}/>{v.power}</span><span>Automatic</span><span>AWD</span></div><div className="mt-5 flex gap-2"><button className="flex-1 rounded-xl bg-gold px-4 py-3 text-sm font-bold text-black">Reserve</button><button className="rounded-xl border border-white/10 px-4 py-3"><ArrowUpRight size={17}/></button></div></div></article>}
